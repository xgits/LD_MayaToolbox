global proc selectHardEdge(){
    string $firstList[] = `ls -sl`;
    if ($firstList[0] == ""){
    SelectToggleMode;
    SelectToggleMode;
    }
    ConvertSelectionToEdges;
    polySelectConstraint -m 2 -t 0x8000 -sm 1;
    string $finaledge[] = `ls -sl`;
    polySelectConstraint -m 0 -dis;
    select $finaledge;
}